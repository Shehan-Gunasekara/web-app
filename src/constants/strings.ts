function useSelectedStrings() {
  const selectedStrings = {
    spaceText: " ",
    appName: "Lono - Local Anywhere",
    metaDescription: "Restaurant management app",
  };

  return selectedStrings;
}

export default useSelectedStrings;
